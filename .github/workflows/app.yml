name: Sample Node js Applications
on:
  push:
    branches:
      - '*'

jobs:
  node-js-demo:
    runs-on: [self-hosted, linux, x64]
    steps:
      - run: pwd
      - run: ls
      - name: mog
        uses: actions/checkout@v2
      - run: pwd
      - run: ls
      - run: sonar-scanner -Dsonar.token=sqa_2a1ddb6a1153cea251fa9707f7b2454f45de3948
